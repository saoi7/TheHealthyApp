import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#000000',
    primary: 'orange',
    secondary: '#414757',
    backgroundColor: "orange",
    error: '#f13a59',
  },
}
